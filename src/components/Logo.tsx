import { cn } from '@/lib/utils'


interface LogoProps {
  className?: string
  size?: 'sm' | 'md' | 'lg' | 'xl'
}

const sizeMap = {
  sm: 'h-8',
  md: 'h-10',
  lg: 'h-12',
  xl: 'h-16'
}

        'rounded-xl bg-primary flex items-center justify-cent
      )}>
      </div>
        'font-bold font-h
        size === 'md' && 'text-xl',
        size === 'xl'
        T
    </div>
}











